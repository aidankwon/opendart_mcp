---
description: Finalize and merge current feature branch
---

1. Check git status to see unstaged/staged changes.
2. Add all changes using `git add .`
3. Commit the changes using `git commit -m "[commit message]"`
4. Checkout the main branch using `git checkout main`
5. Merge the feature branch into main using `git merge [feature-branch-name]`
