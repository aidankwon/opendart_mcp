
services:
  opendart-mcp:
    image: ghcr.io/aidankwon/opendart_mcp:latest
    ports:
      - "3000:3000"
    environment:
      - OPENDART_API_KEY=${OPENDART_API_KEY}
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: always
